{{ form_start(form, {'attr': {'class': 'row'}}) }}
	<div class="input-field col s12 m12 l12">
		{{ form_label(form.titre, 'Titre du journal') }}
		{{ form_widget(form.titre) }}
        
		<span class="error">{{ form_errors(form.titre) }}</span>
	</div>

	<div class="input-field col s12 m6 l6">
		{{ form_label(form.numparution, 'Numéro de parution') }}
		{{ form_widget(form.numparution, {'attr': {'placeholder': '34'}}) }}
		<span class="error">{{ form_errors(form.numparution) }}</span>
	</div>

	<div class="input-field col s12 m12 l12">
		<input type="submit" name="" value="Enregistrer" class="btn btn-primary">
		<input type="submit" value= "Modifier" class="btn btn-warning">
        <input type="button" value= "Supprimer" class="btn btn-danger">
	</div>
{{ form_end(form) }}

